# Copy this file to .env and adjust values as needed
# DO NOT commit .env to git (contains local configuration)

# API Base URL for direct backend access
# Set this to bypass Vite dev-proxy and connect directly to backend
# Required for SSE/EventSource to work reliably
VITE_API_BASE_URL=http://localhost:3000
