# Direct backend URL — fixes EventSource/SSE to /api/channels/events when the Vite proxy returns 404 in the browser.
# Remove this line to use relative /api paths (Vite proxy only). Restart `npm run dev` after changes.
VITE_API_BASE_URL=http://localhost:3000
