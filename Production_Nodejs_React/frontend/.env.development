# Same-origin /api + Vite proxy → backend on the dev machine. Required when the browser runs
# on another host (e.g. PC) and only port 5173 is forwarded from the laptop.
# Uncomment only if your browser can reach the backend directly (e.g. you also forward :3000).
# VITE_API_BASE_URL=http://localhost:3000
