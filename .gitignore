# Local IDE capture path on disk (operator machine; may contain sensitive paths)
**/ide_capture_settings.json

# Environment secrets — keep only templates (e.g. .env.example) in Git.
**/.env
**/.env.*.bak
**/*.bak
